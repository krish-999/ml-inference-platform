services:
  torchserve:
    build:
      context: .
      dockerfile: torchserve/Dockerfile
    networks:
      - ml-platform
    volumes:
      - model-store:/model-store
      - shared-config:/config
    ports:
      - "8080:8080"
      - "8082:8082"

networks:
  ml-platform:
    driver: bridge

volumes:
  model-store:
  shared-config:
